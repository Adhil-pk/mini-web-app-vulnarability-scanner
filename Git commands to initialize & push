git init
git add .
git commit -m "Initial commit: mini webapp vuln scanner (safe marker-based)"
# create remote on GitHub first, then:
git remote add origin git@github.com:yourusername/webapp-vuln-scanner.git
git branch -M main
git push -u origin main
